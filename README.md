# AI Game Genetic Algorithm

This project implements a genetic algorithm to evolve an agent that navigates a grid-based game, collecting items while managing movement costs on varied terrain. It uses Pyxel for rendering, Matplotlib for plotting, and NumPy for numerical operations.

## Installation

Install the required dependencies:

```bash
pip install -r requirements.txt

# Run Game
python main.py
